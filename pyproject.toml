[project]
name = "photoreal_factory"
version = "0.1.0"
description = "Photoreal Factory for ComfyUI"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"